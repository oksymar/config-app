import styled from "styled-components";

export const StyledConsoleArea = styled.div`
  overflow-y: auto;
  overflow-x: hidden;
  max-width: inherit;
  height: 400px;
  margin-bottom: 20px;
`;

export const StyledLine = styled.div`
  overflow-anchor: none;
  word-break: break-all;
  margin-right: 10px;
`;
